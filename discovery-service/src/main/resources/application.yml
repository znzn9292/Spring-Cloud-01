# Eureka Server Port
server:
  port: 8761

# 애플리케이션 고유 네임
spring:
  application:
    name: discovery-service

# Eureka Client
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false